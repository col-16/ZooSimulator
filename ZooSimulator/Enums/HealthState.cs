namespace ZooSimulator.Enums
{
    /// <summary>
    /// The possible health states which can be represented by each Zoo animal.
    /// </summary>
    public enum HealthState
    {
        Alive,
        Immobile,
        Dead
    }
}
